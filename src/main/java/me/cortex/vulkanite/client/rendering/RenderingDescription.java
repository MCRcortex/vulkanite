package me.cortex.vulkanite.client.rendering;

//Describes the full render layout, datapoints, syncpoints, everything
// this then gets built into a VulkanRenderer pipeline and executed
public class RenderingDescription {
}
